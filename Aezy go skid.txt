-- SERVICES
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")
local Lighting = game:GetService("Lighting")
local Stats = game:GetService("Stats")

-- INIT
local lp = Players.LocalPlayer
local character, hrp, hum

local function setupCharacter(char)
    character = char
    hrp = char:WaitForChild("HumanoidRootPart")
    hum = char:WaitForChild("Humanoid")
end
if lp.Character then setupCharacter(lp.Character) end
lp.CharacterAdded:Connect(setupCharacter)

-- THEME
local NK = {
    BLACK       = Color3.fromRGB(0,   0,   0),
    DARK        = Color3.fromRGB(10,  10,  10),
    PANEL       = Color3.fromRGB(22,  22,  22),
    CARD        = Color3.fromRGB(35,  35,  35),
    BLUE        = Color3.fromRGB(0,   56,  168),
    RED         = Color3.fromRGB(206, 17,  38),
    WHITE       = Color3.fromRGB(255, 255, 255),
    GREY        = Color3.fromRGB(160, 160, 160),
    GREY_DARK   = Color3.fromRGB(60,  60,  60),
    GREEN       = Color3.fromRGB(50,  205, 80),
}

-- ============================================================
-- FEATURE LOGIC (ported from Noreal)
-- ============================================================

--// OPTIMIZER
local optimizerEnabled = false
local savedLighting = {}
local optimized = {}
local function enableOptimizer()
    if optimizerEnabled then return end
    optimizerEnabled = true
    savedLighting = {
        GlobalShadows = Lighting.GlobalShadows,
        FogStart = Lighting.FogStart, FogEnd = Lighting.FogEnd,
        Brightness = Lighting.Brightness,
        EnvironmentDiffuseScale = Lighting.EnvironmentDiffuseScale,
        EnvironmentSpecularScale = Lighting.EnvironmentSpecularScale
    }
    Lighting.GlobalShadows = false; Lighting.FogStart = 0; Lighting.FogEnd = 1e9
    Lighting.Brightness = 1; Lighting.EnvironmentDiffuseScale = 0; Lighting.EnvironmentSpecularScale = 0
    for _, v in ipairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") then
            optimized[v] = {v.Material, v.Reflectance}
            v.Material = Enum.Material.Plastic; v.Reflectance = 0
        elseif v:IsA("Decal") or v:IsA("Texture") then
            optimized[v] = v.Transparency; v.Transparency = 1
        elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or v:IsA("Fire") then
            optimized[v] = v.Enabled; v.Enabled = false
        end
    end
end
local function disableOptimizer()
    if not optimizerEnabled then return end
    optimizerEnabled = false
    for k, v in pairs(savedLighting) do Lighting[k] = v end
    for obj, val in pairs(optimized) do
        if obj and obj.Parent then
            if typeof(val) == "table" then obj.Material = val[1]; obj.Reflectance = val[2]
            elseif typeof(val) == "boolean" then obj.Enabled = val
            else obj.Transparency = val end
        end
    end
    optimized = {}
end

--// XRAY BASE
local baseOriginalTransparencies = {}
local plotConnections = {}
local xrayConnection
local function applyXray(plot)
    if baseOriginalTransparencies[plot] then return end
    baseOriginalTransparencies[plot] = {}
    for _, part in ipairs(plot:GetDescendants()) do
        if part:IsA("BasePart") and part.Transparency < 0.6 then
            baseOriginalTransparencies[plot][part] = part.Transparency
            part.Transparency = 0.68
        end
    end
    plotConnections[plot] = plot.DescendantAdded:Connect(function(desc)
        if desc:IsA("BasePart") and desc.Transparency < 0.6 then
            baseOriginalTransparencies[plot][desc] = desc.Transparency
            desc.Transparency = 0.68
        end
    end)
end
local function toggleESPBases(enable)
    local plots = workspace:FindFirstChild("Plots"); if not plots then return end
    if not enable then
        for _, conn in pairs(plotConnections) do conn:Disconnect() end
        plotConnections = {}
        if xrayConnection then xrayConnection:Disconnect(); xrayConnection = nil end
        for _, parts in pairs(baseOriginalTransparencies) do
            for part, original in pairs(parts) do
                if part and part.Parent then part.Transparency = original end
            end
        end
        baseOriginalTransparencies = {}; return
    end
    for _, plot in ipairs(plots:GetChildren()) do applyXray(plot) end
    xrayConnection = plots.ChildAdded:Connect(function(newPlot)
        task.wait(0.2); applyXray(newPlot)
    end)
end

--// ESP PLAYERS
local espConnections = {}
local espEnabled = false
local function createESP(plr)
    if plr == lp then return end
    if not plr.Character then return end
    local char = plr.Character
    local hrpE = char:FindFirstChild("HumanoidRootPart")
    local head = char:FindFirstChild("Head")
    if not (hrpE and head) then return end
    if char:FindFirstChild("HazeeESP") then return end
    local hl = Instance.new("Highlight"); hl.Name = "HazeeESP"
    hl.FillColor = NK.BLUE; hl.OutlineColor = NK.WHITE
    hl.FillTransparency = 0.3; hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop; hl.Parent = char
    local bb = Instance.new("BillboardGui"); bb.Name = "AezyName"
    bb.Adornee = head; bb.Size = UDim2.new(0, 200, 0, 50)
    bb.StudsOffset = Vector3.new(0, 3, 0); bb.AlwaysOnTop = true; bb.Parent = char
    local lbl = Instance.new("TextLabel"); lbl.Size = UDim2.new(1,0,1,0)
    lbl.BackgroundTransparency = 1; lbl.Text = plr.DisplayName or plr.Name
    lbl.TextColor3 = NK.WHITE; lbl.Font = Enum.Font.GothamBold
    lbl.TextScaled = true; lbl.TextStrokeTransparency = 0.4
    lbl.TextStrokeColor3 = NK.BLUE; lbl.Parent = bb
end
local function removeESP(plr)
    if not plr.Character then return end
    local c = plr.Character
    for _, n in ipairs({"HazeeESP","HazeeName"}) do
        local x = c:FindFirstChild(n); if x then x:Destroy() end
    end
end
local function toggleESPPlayers(enable)
    espEnabled = enable
    if enable then
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= lp then
                if plr.Character then createESP(plr) end
                local conn = plr.CharacterAdded:Connect(function()
                    task.wait(0.2); if espEnabled then createESP(plr) end
                end)
                table.insert(espConnections, conn)
            end
        end
        local pac = Players.PlayerAdded:Connect(function(plr)
            if plr == lp then return end
            local cac = plr.CharacterAdded:Connect(function()
                task.wait(0.2); if espEnabled then createESP(plr) end
            end)
            table.insert(espConnections, cac)
        end)
        table.insert(espConnections, pac)
    else
        for _, plr in ipairs(Players:GetPlayers()) do removeESP(plr) end
        for _, conn in ipairs(espConnections) do
            if conn and conn.Connected then conn:Disconnect() end
        end
        espConnections = {}
    end
end

--// ANTI SENTRY
local antiSentryConnection, antiSentryTarget
local DETECTION_DISTANCE = 60
local PULL_DISTANCE = -5
local function getCharacter() return lp.Character end
local function getWeapon()
    local char = getCharacter(); if not char then return nil end
    return lp.Backpack:FindFirstChild("Bat") or char:FindFirstChild("Bat")
end
local function findSentryTarget()
    local char = getCharacter(); if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local rootPos = char.HumanoidRootPart.Position
    for _, obj in pairs(workspace:GetChildren()) do
        if obj.Name:find("Sentry") and not obj.Name:lower():find("bullet") then
            local ownerId = obj.Name:match("Sentry_(%d+)")
            if ownerId and tonumber(ownerId) == lp.UserId then continue end
            local part = obj:IsA("BasePart") and obj
                or obj:IsA("Model") and (obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart"))
            if part and (rootPos - part.Position).Magnitude <= DETECTION_DISTANCE then return obj end
        end
    end
end
local function moveSentry(obj)
    local char = getCharacter(); if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    for _, part in pairs(obj:GetDescendants()) do
        if part:IsA("BasePart") then part.CanCollide = false end
    end
    local root = char.HumanoidRootPart
    local cf = root.CFrame * CFrame.new(0, 0, PULL_DISTANCE)
    if obj:IsA("BasePart") then obj.CFrame = cf
    elseif obj:IsA("Model") then
        local m = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")
        if m then m.CFrame = cf end
    end
end
local function attackSentry()
    local char = getCharacter(); if not char then return end
    local hum2 = char:FindFirstChildOfClass("Humanoid"); if not hum2 then return end
    local weapon = getWeapon(); if not weapon then return end
    if weapon.Parent == lp.Backpack then hum2:EquipTool(weapon); task.wait(0.1) end
    local handle = weapon:FindFirstChild("Handle")
    if handle then handle.CanCollide = false end
    pcall(function() weapon:Activate() end)
    for _, r in pairs(weapon:GetDescendants()) do
        if r:IsA("RemoteEvent") then pcall(function() r:FireServer() end) end
    end
end
local function startAntiSentry()
    if antiSentryConnection then return end
    antiSentryConnection = RunService.Heartbeat:Connect(function()
        if antiSentryTarget and antiSentryTarget.Parent == workspace then
            moveSentry(antiSentryTarget); attackSentry()
        else antiSentryTarget = findSentryTarget() end
    end)
end
local function stopAntiSentry()
    if antiSentryConnection then antiSentryConnection:Disconnect(); antiSentryConnection = nil end
    antiSentryTarget = nil
end

--// ANTI BEE & DISCO
local ANTI_BEE_DISCO = { enabled = false, connections = {}, originalMoveFunction = nil, controlsProtected = false }
local BAD_LIGHTING_NAMES = { Blue = true, DiscoEffect = true, BeeBlur = true, ColorCorrection = true }
local function antiBeeDiscoNuke(obj)
    if not obj or not obj.Parent then return end
    if BAD_LIGHTING_NAMES[obj.Name] then pcall(function() obj:Destroy() end) end
end
local function antiBeeDiscoDisconnectAll()
    for _, conn in ipairs(ANTI_BEE_DISCO.connections) do
        if typeof(conn) == "RBXScriptConnection" then conn:Disconnect() end
    end
    ANTI_BEE_DISCO.connections = {}
end
local function enableAntiBee()
    if ANTI_BEE_DISCO.enabled then return end
    ANTI_BEE_DISCO.enabled = true
    for _, inst in ipairs(Lighting:GetDescendants()) do antiBeeDiscoNuke(inst) end
    local addedConn = Lighting.DescendantAdded:Connect(function(obj)
        if ANTI_BEE_DISCO.enabled then antiBeeDiscoNuke(obj) end
    end)
    table.insert(ANTI_BEE_DISCO.connections, addedConn)
end
local function disableAntiBee()
    if not ANTI_BEE_DISCO.enabled then return end
    ANTI_BEE_DISCO.enabled = false; antiBeeDiscoDisconnectAll()
end

--// ANTI FPS DEVOURER
local ANTI_FPS = { enabled = false, connections = {}, hiddenAccessories = {} }
local function enableAntiFPS()
    if ANTI_FPS.enabled then return end
    ANTI_FPS.enabled = true
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Accessory") then
            ANTI_FPS.hiddenAccessories[obj] = obj.Parent; obj.Parent = nil
        end
    end
    local conn = workspace.DescendantAdded:Connect(function(obj)
        if ANTI_FPS.enabled and obj:IsA("Accessory") then
            ANTI_FPS.hiddenAccessories[obj] = obj.Parent; obj.Parent = nil
        end
    end)
    table.insert(ANTI_FPS.connections, conn)
end
local function disableAntiFPS()
    if not ANTI_FPS.enabled then return end
    ANTI_FPS.enabled = false
    for _, conn in ipairs(ANTI_FPS.connections) do
        if typeof(conn) == "RBXScriptConnection" then conn:Disconnect() end
    end
    ANTI_FPS.connections = {}
    for acc, orig in pairs(ANTI_FPS.hiddenAccessories) do
        if acc then acc.Parent = orig end
    end
    ANTI_FPS.hiddenAccessories = {}
end

--// ANTI RAGDOLL
local antiRagdollMode = nil
local ragdollConnections = {}
local cachedCharData = {}
local function cacheCharacterData()
    local char = lp.Character; if not char then return false end
    local hum2 = char:FindFirstChildOfClass("Humanoid")
    local root = char:FindFirstChild("HumanoidRootPart")
    if not hum2 or not root then return false end
    cachedCharData = { character = char, humanoid = hum2, root = root }
    return true
end
local function disconnectAllRagdoll()
    for _, conn in ipairs(ragdollConnections) do
        pcall(function() conn:Disconnect() end)
    end
    ragdollConnections = {}
end
local function isRagdolled()
    if not cachedCharData.humanoid then return false end
    local state = cachedCharData.humanoid:GetState()
    return state == Enum.HumanoidStateType.Physics
        or state == Enum.HumanoidStateType.Ragdoll
        or state == Enum.HumanoidStateType.FallingDown
end
local function forceExitRagdoll()
    if not cachedCharData.humanoid or not cachedCharData.root then return end
    if cachedCharData.humanoid.Health > 0 then
        cachedCharData.humanoid:ChangeState(Enum.HumanoidStateType.Running)
    end
    cachedCharData.root.Anchored = false
    cachedCharData.root.AssemblyLinearVelocity = Vector3.zero
    cachedCharData.root.AssemblyAngularVelocity = Vector3.zero
end
local function removeRagdollConstraints()
    if not cachedCharData.character then return end
    for _, d in ipairs(cachedCharData.character:GetDescendants()) do
        if d:IsA("BallSocketConstraint") or
            (d:IsA("Attachment") and d.Name:find("RagdollAttachment")) then
            pcall(function() d:Destroy() end)
        end
    end
end
local function antiRagdollLoop()
    while antiRagdollMode do
        task.wait()
        if isRagdolled() then removeRagdollConstraints(); forceExitRagdoll() end
        local cam = workspace.CurrentCamera
        if cam and cachedCharData.humanoid then
            if cam.CameraSubject ~= cachedCharData.humanoid then
                cam.CameraSubject = cachedCharData.humanoid
            end
        end
    end
end
local function toggleAntiRagdoll(enable)
    if enable then
        disconnectAllRagdoll()
        if not cacheCharacterData() then return end
        antiRagdollMode = true
        local charConn = lp.CharacterAdded:Connect(function()
            task.wait(0.5); if antiRagdollMode then cacheCharacterData() end
        end)
        table.insert(ragdollConnections, charConn)
        task.spawn(antiRagdollLoop)
    else
        antiRagdollMode = nil
        disconnectAllRagdoll()
        cachedCharData = {}
    end
end

--// MELEE AIMBOT
local MELEE_RANGE = 45
local meleeEnabled = false
local meleeConnection, meleeAlignOrientation, meleeAttachment
local function getClosestMeleeTarget(hrpRef)
    local closest, minDist = nil, MELEE_RANGE
    for _, p in ipairs(Players:GetPlayers()) do
        if p == lp then continue end
        local char2 = p.Character; if not char2 then continue end
        local targetHrp = char2:FindFirstChild("HumanoidRootPart")
        local hum2 = char2:FindFirstChildOfClass("Humanoid")
        if not (hum2 and targetHrp) or hum2.Health <= 0 then continue end
        local dist = (targetHrp.Position - hrpRef.Position).Magnitude
        if dist < minDist then minDist = dist; closest = targetHrp end
    end
    return closest
end
local function createMeleeAimbot(char)
    local hrpRef = char:WaitForChild("HumanoidRootPart", 8)
    local humanoidRef = char:WaitForChild("Humanoid", 8)
    if not (hrpRef and humanoidRef) then return end
    if meleeAlignOrientation then pcall(function() meleeAlignOrientation:Destroy() end) end
    if meleeAttachment then pcall(function() meleeAttachment:Destroy() end) end
    meleeAttachment = Instance.new("Attachment"); meleeAttachment.Parent = hrpRef
    meleeAlignOrientation = Instance.new("AlignOrientation")
    meleeAlignOrientation.Attachment0 = meleeAttachment
    meleeAlignOrientation.Mode = Enum.OrientationAlignmentMode.OneAttachment
    meleeAlignOrientation.RigidityEnabled = true
    meleeAlignOrientation.MaxTorque = 100000
    meleeAlignOrientation.Responsiveness = 200
    meleeAlignOrientation.Parent = hrpRef
    if meleeConnection then meleeConnection:Disconnect() end
    meleeConnection = RunService.RenderStepped:Connect(function()
        if not char.Parent or not meleeEnabled then return end
        local target = getClosestMeleeTarget(hrpRef)
        if target then
            humanoidRef.AutoRotate = false
            meleeAlignOrientation.Enabled = true
            local targetPos = Vector3.new(target.Position.X, hrpRef.Position.Y, target.Position.Z)
            meleeAlignOrientation.CFrame = CFrame.lookAt(hrpRef.Position, targetPos)
        else
            meleeAlignOrientation.Enabled = false
            humanoidRef.AutoRotate = true
        end
    end)
end
local function disableMeleeAimbot()
    meleeEnabled = false
    if meleeConnection then meleeConnection:Disconnect(); meleeConnection = nil end
    if meleeAlignOrientation then
        meleeAlignOrientation.Enabled = false
        pcall(function() meleeAlignOrientation:Destroy() end); meleeAlignOrientation = nil
    end
    if character and character:FindFirstChild("Humanoid") then
        character.Humanoid.AutoRotate = true
    end
    if meleeAttachment then pcall(function() meleeAttachment:Destroy() end); meleeAttachment = nil end
end
lp.CharacterAdded:Connect(function(char)
    if meleeEnabled then task.spawn(function() task.wait(0.3); createMeleeAimbot(char) end) end
end)

--// SMART AUTO STEAL
local autoStealEnabled = false
local stealLoopThread = nil
local STEAL_GRAB_RADIUS = 50
local grabRadius = 50
local function isMyBase(plotName)
    local plots = workspace:FindFirstChild("Plots"); if not plots then return false end
    local plot = plots:FindFirstChild(plotName); if not plot then return false end
    local sign = plot:FindFirstChild("PlotSign")
    local yourBase = sign and sign:FindFirstChild("YourBase")
    return yourBase and yourBase.Enabled == true
end
local function scanStealTargets()
    local results = {}
    local plots = workspace:FindFirstChild("Plots"); if not plots then return results end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:IsA("Model") and not isMyBase(plot.Name) then
            local podiums = plot:FindFirstChild("AnimalPodiums")
            if podiums then
                for _, podium in ipairs(podiums:GetChildren()) do
                    if podium:IsA("Model") then
                        local base = podium:FindFirstChild("Base")
                        if base then
                            local spawnAtt = base:FindFirstChild("Spawn")
                            local att = spawnAtt and spawnAtt:FindFirstChild("PromptAttachment")
                            local prompt = att and att:FindFirstChildOfClass("ProximityPrompt")
                            if prompt and prompt.Enabled then
                                table.insert(results, { prompt = prompt, position = base.Position, plot = plot.Name })
                            end
                        end
                    end
                end
            end
        end
    end
    return results
end
local function snapSteal(target)
    local char = lp.Character; if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
    local savedCFrame = root.CFrame
    local podiumPos = target.position
    local offsetDir = (root.Position - podiumPos)
    offsetDir = Vector3.new(offsetDir.X, 0, offsetDir.Z)
    if offsetDir.Magnitude < 0.1 then offsetDir = Vector3.new(1, 0, 0) end
    offsetDir = offsetDir.Unit
    local stealPos = podiumPos + offsetDir * 2.5
    root.CFrame = CFrame.new(Vector3.new(stealPos.X, podiumPos.Y + 3, stealPos.Z))
    task.wait(0.05)
    pcall(function() fireproximityprompt(target.prompt) end)
    task.wait(0.05)
    pcall(function() fireproximityprompt(target.prompt) end)
    root.CFrame = savedCFrame
end
local function startSmartSteal()
    if stealLoopThread then return end
    stealLoopThread = task.spawn(function()
        while autoStealEnabled do
            local char = lp.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            if root then
                local targets = scanStealTargets()
                local best, bestDist = nil, STEAL_GRAB_RADIUS
                for _, t in ipairs(targets) do
                    local d = (root.Position - t.position).Magnitude
                    if d < bestDist then bestDist = d; best = t end
                end
                if best then snapSteal(best) end
            end
            task.wait(0.3)
        end
        stealLoopThread = nil
    end)
end
local function stopSmartSteal()
    autoStealEnabled = false
    if stealLoopThread then task.cancel(stealLoopThread); stealLoopThread = nil end
end

--// STEAL RADIUS VISUAL
local stealSquarePart = nil
local function hideSquare()
    if stealSquarePart then stealSquarePart:Destroy(); stealSquarePart = nil end
end
local function createOrUpdateSquare(radius)
    if not stealSquarePart then
        stealSquarePart = Instance.new("Part")
        stealSquarePart.Name = "HazeeStealCircle"
        stealSquarePart.Anchored = true; stealSquarePart.CanCollide = false
        stealSquarePart.Transparency = 0.7; stealSquarePart.Material = Enum.Material.Neon
        stealSquarePart.Color = NK.BLUE
        stealSquarePart.Shape = Enum.PartType.Cylinder
        stealSquarePart.Size = Vector3.new(0.05, radius * 2, radius * 2)
        stealSquarePart.Parent = workspace
    else
        stealSquarePart.Size = Vector3.new(0.05, radius * 2, radius * 2)
    end
end
local function updateSquarePosition()
    if stealSquarePart and lp.Character then
        local root = lp.Character:FindFirstChild("HumanoidRootPart")
        if root then
            stealSquarePart.CFrame = CFrame.new(root.Position + Vector3.new(0, -2.5, 0))
                * CFrame.Angles(0, 0, math.rad(90))
        end
    end
end

--// SLOW FALL
local slowFallEnabled = false
RunService.Heartbeat:Connect(function()
    if not slowFallEnabled then return end
    local char = lp.Character; if not char then return end
    local h = char:FindFirstChildOfClass("Humanoid")
    local root = char:FindFirstChild("HumanoidRootPart")
    if not h or not root then return end
    if h:GetState() == Enum.HumanoidStateType.Freefall then
        local v = root.AssemblyLinearVelocity
        if v.Y < -1 then root.AssemblyLinearVelocity = Vector3.new(v.X, v.Y * 0.5, v.Z) end
    end
end)

--// LOCK TARGET
local LOCK_RADIUS = 70
local LOCK_SPEED = 50
local lockHbConn, lockLv, lockAtt, lockGyro
local lockEnabled = false
local function getNearest()
    local char = lp.Character; if not char then return nil end
    local hrpRef = char:FindFirstChild("HumanoidRootPart"); if not hrpRef then return nil end
    local nearest, nearestDist = nil, LOCK_RADIUS
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= lp then
            local pc = plr.Character
            local phrp = pc and pc:FindFirstChild("HumanoidRootPart")
            if phrp then
                local d = (phrp.Position - hrpRef.Position).Magnitude
                if d <= nearestDist then nearest = plr; nearestDist = d end
            end
        end
    end
    return nearest
end
local function getBat()
    for _, t in ipairs(lp.Backpack:GetChildren()) do
        if t:IsA("Tool") and t.Name:lower():find("bat") then return t end
    end
    local char = lp.Character
    if char then
        for _, t in ipairs(char:GetChildren()) do
            if t:IsA("Tool") and t.Name:lower():find("bat") then return t end
        end
    end
    return nil
end
local function startLock()
    local char = lp.Character; if not char then return end
    local hrpRef = char:FindFirstChild("HumanoidRootPart"); if not hrpRef then return end
    lockAtt = Instance.new("Attachment", hrpRef)
    lockLv = Instance.new("LinearVelocity", hrpRef)
    lockLv.Attachment0 = lockAtt; lockLv.MaxForce = 50000
    lockLv.RelativeTo = Enum.ActuatorRelativeTo.World; lockLv.Enabled = false
    lockGyro = Instance.new("AlignOrientation", hrpRef)
    lockGyro.Attachment0 = lockAtt; lockGyro.MaxTorque = 50000
    lockGyro.Responsiveness = 120; lockGyro.Enabled = false
    lockHbConn = RunService.Heartbeat:Connect(function()
        local tp = getNearest()
        if not tp then lockLv.Enabled = false; lockGyro.Enabled = false; return end
        local tChar = tp.Character
        local tHrp = tChar and tChar:FindFirstChild("HumanoidRootPart")
        if not tHrp then lockLv.Enabled = false; lockGyro.Enabled = false; return end
        lockLv.Enabled = true; lockGyro.Enabled = true
        local frontPos = tHrp.Position + tHrp.CFrame.LookVector * 2.2
        local dir = frontPos - hrpRef.Position
        lockLv.VectorVelocity = dir.Magnitude > 0.5 and dir.Unit * LOCK_SPEED or Vector3.zero
        lockGyro.CFrame = CFrame.lookAt(hrpRef.Position, frontPos)
        local bat = getBat()
        if bat then
            if bat.Parent ~= char then char.Humanoid:EquipTool(bat) end
            bat:Activate()
        end
    end)
end
local function stopLock()
    if lockHbConn then lockHbConn:Disconnect(); lockHbConn = nil end
    if lockLv then lockLv:Destroy(); lockLv = nil end
    if lockGyro then lockGyro:Destroy(); lockGyro = nil end
    if lockAtt then lockAtt:Destroy(); lockAtt = nil end
end

--// MEDUSA
local MEDUSA_RADIUS = 10
local SPAM_DELAY = 0.15
local medusaPart, lastUse, AutoMedusaEnabled, MedusaInitialized = nil, 0, false, false
local function InitMedusa()
    if MedusaInitialized then return end
    MedusaInitialized = true
    medusaPart = Instance.new("Part"); medusaPart.Name = "HazeeMedusaRadius"
    medusaPart.Anchored = true; medusaPart.CanCollide = false
    medusaPart.Transparency = 1; medusaPart.Material = Enum.Material.Neon
    medusaPart.Color = NK.BLUE; medusaPart.Shape = Enum.PartType.Cylinder
    medusaPart.Size = Vector3.new(0.05, MEDUSA_RADIUS * 2, MEDUSA_RADIUS * 2)
    medusaPart.Parent = workspace
    RunService.RenderStepped:Connect(function()
        if not AutoMedusaEnabled then medusaPart.Transparency = 1; return end
        medusaPart.Transparency = 0.75
        local char = lp.Character
        local root = char and char:FindFirstChild("HumanoidRootPart"); if not root then return end
        medusaPart.CFrame = CFrame.new(root.Position + Vector3.new(0, -2.5, 0))
            * CFrame.Angles(0, 0, math.rad(90))
    end)
    RunService.Heartbeat:Connect(function()
        if not AutoMedusaEnabled then return end
        local char = lp.Character
        local root = char and char:FindFirstChild("HumanoidRootPart"); if not root then return end
        local tool
        for _, t in ipairs(char:GetChildren()) do
            if t:IsA("Tool") and t.Name == "Medusa's Head" then tool = t; break end
        end
        if not tool or tick() - lastUse < SPAM_DELAY then return end
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= lp then
                local pChar = p.Character
                local pRoot = pChar and pChar:FindFirstChild("HumanoidRootPart")
                if pRoot and (pRoot.Position - root.Position).Magnitude <= MEDUSA_RADIUS then
                    tool:Activate(); lastUse = tick(); break
                end
            end
        end
    end)
end

--// SPIN BODY
local spinForce
local function startSpinBody()
    local char = lp.Character; if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart"); if not root or spinForce then return end
    spinForce = Instance.new("BodyAngularVelocity"); spinForce.Name = "HazeeSpinForce"
    spinForce.AngularVelocity = Vector3.new(0, 25, 0)
    spinForce.MaxTorque = Vector3.new(0, math.huge, 0)
    spinForce.P = 1250; spinForce.Parent = root
end
local function stopSpinBody()
    if spinForce then spinForce:Destroy(); spinForce = nil end
end

--// NO WALK ANIMATION
local savedAnimate = nil
local function enableNoWalkAnim()
    local char = lp.Character; if not char then return end
    local animate = char:FindFirstChild("Animate")
    if animate then savedAnimate = animate; animate.Disabled = true end
    local h = char:FindFirstChildOfClass("Humanoid")
    if h then for _, t in ipairs(h:GetPlayingAnimationTracks()) do t:Stop() end end
end
local function disableNoWalkAnim()
    if savedAnimate then savedAnimate.Disabled = false; savedAnimate = nil end
end

--// AUTO BAT
local autoBatActive = false
local autoBatLoop = nil

--// INFINITE JUMP
local infiniteJumpEnabled = false
UserInputService.JumpRequest:Connect(function()
    if not infiniteJumpEnabled then return end
    local char = lp.Character; if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
    root.AssemblyLinearVelocity = Vector3.new(
        root.AssemblyLinearVelocity.X, 50, root.AssemblyLinearVelocity.Z)
end)

--// AUTO DUEL
local DuelState = { Moving = false, Waypoints = {}, CurrentWaypoint = 1, Connection = nil }
local function getHRP()
    local char = lp.Character
    return char and (char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("UpperTorso"))
end
local duelStatusLabel -- forward ref, set after GUI build
local function stopDuel()
    if DuelState.Connection then DuelState.Connection:Disconnect(); DuelState.Connection = nil end
    DuelState.Moving = false; DuelState.CurrentWaypoint = 1; DuelState.Waypoints = {}
end
local function startDuel()
    local root = getHRP(); if not root then return end
    DuelState.Moving = true
    if (root.Position - Vector3.new(-475,-7,96)).Magnitude
        > (root.Position - Vector3.new(-474,-7,23)).Magnitude then
        DuelState.Waypoints = {
            {position=Vector3.new(-475,-7,96),speed=60.5},{position=Vector3.new(-483,-5,95),speed=60.5},
            {position=Vector3.new(-487,-5,95),speed=60.5},{position=Vector3.new(-492,-5,95),speed=30},
            {position=Vector3.new(-473,-7,95),speed=30},{position=Vector3.new(-473,-7,11),speed=30},
        }
    else
        DuelState.Waypoints = {
            {position=Vector3.new(-474,-7,23),speed=60.5},{position=Vector3.new(-484,-5,24),speed=60.5},
            {position=Vector3.new(-488,-5,24),speed=60.5},{position=Vector3.new(-493,-5,25),speed=30},
            {position=Vector3.new(-473,-7,25),speed=30},{position=Vector3.new(-474,-7,112),speed=30},
        }
    end
    DuelState.CurrentWaypoint = 1
    DuelState.Connection = RunService.Stepped:Connect(function()
        if not DuelState.Moving then return end
        local hrpRef = getHRP(); if not hrpRef then stopDuel(); return end
        local wp = DuelState.Waypoints[DuelState.CurrentWaypoint]; if not wp then stopDuel(); return end
        local dist = (hrpRef.Position - wp.position).Magnitude
        if dist < 6 then
            if DuelState.CurrentWaypoint >= #DuelState.Waypoints then stopDuel(); return end
            DuelState.CurrentWaypoint += 1
        else
            local dir = (wp.position - hrpRef.Position).Unit
            hrpRef.Velocity = Vector3.new(dir.X * wp.speed, hrpRef.Velocity.Y, dir.Z * wp.speed)
        end
    end)
end
lp.CharacterAdded:Connect(function() if DuelState.Moving then task.wait(0.3); stopDuel() end end)

-- ============================================================
-- SCREEN GUI
-- ============================================================
local HazeeDUELGUI = Instance.new('ScreenGui')
HazeeDUELGUI.Name = 'HazeeDUELGUI'
HazeeDUELGUI.ResetOnSpawn = false
HazeeDUELGUI.DisplayOrder = 10
HazeeDUELGUI.Parent = lp:WaitForChild("PlayerGui")

-- Progress bar (steal)
local progressBarBg = Instance.new("Frame")
progressBarBg.Size = UDim2.new(0, 240, 0, 10)
progressBarBg.Position = UDim2.new(0.5, -120, 0, 52)
progressBarBg.BackgroundColor3 = NK.CARD; progressBarBg.BackgroundTransparency = 0.1
progressBarBg.Visible = false; progressBarBg.Parent = HazeeDUELGUI
Instance.new("UICorner", progressBarBg).CornerRadius = UDim.new(1, 0)
local progressFill = Instance.new("Frame")
progressFill.Size = UDim2.new(0, 0, 1, 0); progressFill.BackgroundColor3 = NK.BLUE
progressFill.BorderSizePixel = 0; progressFill.Parent = progressBarBg
Instance.new("UICorner", progressFill).CornerRadius = UDim.new(1, 0)
local percentLabel = Instance.new("TextLabel")
percentLabel.Size = UDim2.new(1,0,1,0); percentLabel.BackgroundTransparency = 1
percentLabel.Font = Enum.Font.GothamBold; percentLabel.TextSize = 11
percentLabel.TextColor3 = NK.WHITE; percentLabel.Text = "0%"; percentLabel.Parent = progressBarBg

-- FPS/Ping bar
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(0, 260, 0, 34)
topBar.Position = UDim2.new(0.5, -130, 0, 12)
topBar.BackgroundColor3 = NK.DARK; topBar.BorderSizePixel = 0; topBar.Parent = HazeeDUELGUI
Instance.new("UICorner", topBar).CornerRadius = UDim.new(0, 10)
local topStroke = Instance.new("UIStroke", topBar); topStroke.Color = NK.BLUE; topStroke.Thickness = 1.5
local topLeft = Instance.new("TextLabel")
topLeft.Size = UDim2.new(0, 34, 1, 0); topLeft.BackgroundTransparency = 1
topLeft.Text = "🇵🇭"; topLeft.Font = Enum.Font.GothamBold; topLeft.TextSize = 16
topLeft.TextColor3 = NK.RED; topLeft.Parent = topBar
local topLabel = Instance.new("TextLabel")
topLabel.Size = UDim2.new(1,-40,1,0); topLabel.Position = UDim2.new(0,36,0,0)
topLabel.BackgroundTransparency = 1; topLabel.Font = Enum.Font.GothamBold; topLabel.TextSize = 13
topLabel.TextColor3 = NK.WHITE; topLabel.TextXAlignment = Enum.TextXAlignment.Left; topLabel.Parent = topBar
local fps, framesCount, lastT = 60, 0, tick()
RunService.RenderStepped:Connect(function()
    framesCount += 1
    if tick() - lastT >= 1 then fps = framesCount; framesCount = 0; lastT = tick() end
    local ping = 0
    local network = Stats:FindFirstChild("Network")
    if network and network:FindFirstChild("ServerStatsItem") then
        local dp = network.ServerStatsItem:FindFirstChild("Data Ping")
        if dp then ping = math.floor(dp:GetValue()) end
    end
    topLabel.Text = "Hazee DUEL  |  " .. fps .. " FPS  |  " .. ping .. " ms"
end)

-- QUICK MENU (right side panel)
local QuickMenu = Instance.new('Frame')
QuickMenu.Name = 'QuickMenu'
QuickMenu.Size = UDim2.new(0, 110, 0, 310)
QuickMenu.Position = UDim2.new(1, -120, 0.5, -155)
QuickMenu.BackgroundColor3 = NK.DARK
QuickMenu.BackgroundTransparency = 0
QuickMenu.BorderSizePixel = 0
QuickMenu.Parent = HazeeDUELGUI
Instance.new("UICorner", QuickMenu).CornerRadius = UDim.new(0, 12)
local qmStroke = Instance.new("UIStroke", QuickMenu)
qmStroke.Color = NK.BLUE; qmStroke.Thickness = 1.5

local qmTitle = Instance.new("TextLabel")
qmTitle.Size = UDim2.new(1, 0, 0, 28)
qmTitle.BackgroundTransparency = 1
qmTitle.Text = "QUICK"; qmTitle.Font = Enum.Font.GothamBold
qmTitle.TextSize = 12; qmTitle.TextColor3 = NK.RED; qmTitle.Parent = QuickMenu

local QuickStates = {
    AutoPlay  = false,
    AutoBat2  = false,
    TPDOWN    = false,
    DropBat   = false,
    CarrySpeed = false,
}

local function createQuickBtn(labelText, key, yPos, onEnable, onDisable)
    local btn = Instance.new('TextButton')
    btn.Size = UDim2.new(1, -12, 0, 44)
    btn.Position = UDim2.new(0, 6, 0, yPos)
    btn.Text = labelText
    btn.TextColor3 = NK.GREY
    btn.BackgroundColor3 = NK.PANEL
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    btn.BorderSizePixel = 0
    btn.Parent = QuickMenu
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    Instance.new("UIStroke", btn).Color = NK.GREY_DARK

    btn.MouseButton1Click:Connect(function()
        QuickStates[key] = not QuickStates[key]
        if QuickStates[key] then
            TweenService:Create(btn, TweenInfo.new(0.15), { BackgroundColor3 = NK.BLUE }):Play()
            btn.TextColor3 = NK.WHITE
            if onEnable then onEnable() end
        else
            TweenService:Create(btn, TweenInfo.new(0.15), { BackgroundColor3 = NK.PANEL }):Play()
            btn.TextColor3 = NK.GREY
            if onDisable then onDisable() end
        end
    end)
    return btn
end

-- AutoPlay (auto accept duel prompts)
local autoPlayConn
createQuickBtn("AUTO\nPLAY", "AutoPlay", 32,
    function()
        autoPlayConn = RunService.Heartbeat:Connect(function()
            for _, v in ipairs(workspace:GetDescendants()) do
                if v:IsA("ProximityPrompt") and v.Enabled then
                    local n = v.Parent and v.Parent.Name or ""
                    if n:lower():find("duel") or n:lower():find("accept") or n:lower():find("queue") then
                        pcall(function() fireproximityprompt(v) end)
                    end
                end
            end
        end)
    end,
    function()
        if autoPlayConn then autoPlayConn:Disconnect(); autoPlayConn = nil end
    end)

-- AutoBat (quick side button)
local autoBat2Loop
createQuickBtn("AUTO\nBAT", "AutoBat2", 82,
    function()
        autoBat2Loop = task.spawn(function()
            while QuickStates.AutoBat2 do
                local char = lp.Character
                if char then
                    local tool = char:FindFirstChild("Bat")
                    if tool and tool:IsA("Tool") then pcall(function() tool:Activate() end) end
                end
                task.wait(0.35)
            end
        end)
    end,
    function() QuickStates.AutoBat2 = false end)

-- TPDOWN (teleport downward)
local tpdownConn
createQuickBtn("TP\nDOWN", "TPDOWN", 132,
    function()
        tpdownConn = RunService.Heartbeat:Connect(function()
            local char = lp.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            if root then root.CFrame = root.CFrame * CFrame.new(0, -0.5, 0) end
        end)
    end,
    function()
        if tpdownConn then tpdownConn:Disconnect(); tpdownConn = nil end
    end)

-- DropBat
createQuickBtn("DROP\nBAT", "DropBat", 182,
    function()
        local char = lp.Character
        if char then
            local tool = char:FindFirstChildOfClass("Tool")
            if tool then
                local h = char:FindFirstChildOfClass("Humanoid")
                if h then h:UnequipTools() end
            end
        end
        QuickStates.DropBat = false
    end,
    nil)

-- CarrySpeed
local carrySpeedConn
createQuickBtn("CARRY\nSPEED", "CarrySpeed", 232,
    function()
        carrySpeedConn = RunService.Heartbeat:Connect(function()
            local char = lp.Character
            local h = char and char:FindFirstChildOfClass("Humanoid")
            if h then h.WalkSpeed = 32 end
        end)
    end,
    function()
        if carrySpeedConn then carrySpeedConn:Disconnect(); carrySpeedConn = nil end
        local char = lp.Character
        local h = char and char:FindFirstChildOfClass("Humanoid")
        if h then h.WalkSpeed = 16 end
    end)

-- FLOATING TOGGLE BUTTON
local MobileToggle = Instance.new('TextButton')
MobileToggle.Size = UDim2.new(0, 54, 0, 54)
MobileToggle.Position = UDim2.new(1, -68, 0, 56)
MobileToggle.Text = '🇵🇭'
MobileToggle.TextSize = 28
MobileToggle.Font = Enum.Font.GothamBold
MobileToggle.TextColor3 = NK.WHITE
MobileToggle.BackgroundColor3 = NK.BLUE
MobileToggle.BorderSizePixel = 0
MobileToggle.Parent = HazeeDUELGUI
Instance.new("UICorner", MobileToggle).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", MobileToggle).Color = NK.RED

-- Draggable floating button
do
    local dragging, dragInput, dragStart, startPos = false, nil, nil, nil
    MobileToggle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = MobileToggle.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    MobileToggle.InputChanged:Connect(function(input)
        if (input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch) and dragging then
            local delta = input.Position - dragStart
            MobileToggle.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- MAIN HUB PANEL
local HUB_WIDTH  = 300
local HUB_HEIGHT = 380
local hub = Instance.new("Frame")
hub.Size = UDim2.new(0, HUB_WIDTH, 0, HUB_HEIGHT)
hub.Position = UDim2.new(0, -(HUB_WIDTH + 20), 0.5, -HUB_HEIGHT / 2)
hub.BackgroundColor3 = NK.DARK; hub.BackgroundTransparency = 0
hub.BorderSizePixel = 0; hub.Parent = HazeeDUELGUI
Instance.new("UICorner", hub).CornerRadius = UDim.new(0, 16)
local hubStroke = Instance.new("UIStroke", hub); hubStroke.Color = NK.BLUE; hubStroke.Thickness = 2

-- Hub header
local hubHeader = Instance.new("Frame")
hubHeader.Size = UDim2.new(1, 0, 0, 54)
hubHeader.BackgroundColor3 = NK.BLACK; hubHeader.BorderSizePixel = 0; hubHeader.Parent = hub
Instance.new("UICorner", hubHeader).CornerRadius = UDim.new(0, 16)
local headerAccent = Instance.new("Frame")
headerAccent.Size = UDim2.new(1, 0, 0, 3); headerAccent.Position = UDim2.new(0, 0, 1, -3)
headerAccent.BackgroundColor3 = NK.RED; headerAccent.BorderSizePixel = 0; headerAccent.Parent = hubHeader
local hubFlag = Instance.new("TextLabel")
hubFlag.Size = UDim2.new(0, 38, 0, 38); hubFlag.Position = UDim2.new(0, 10, 0.5, -19)
hubFlag.BackgroundTransparency = 1; hubFlag.Text = "🇵🇭"
hubFlag.Font = Enum.Font.GothamBold; hubFlag.TextSize = 26; hubFlag.Parent = hubHeader
local hubTitle = Instance.new("TextLabel")
hubTitle.Size = UDim2.new(0, 160, 0, 28); hubTitle.Position = UDim2.new(0, 52, 0, 6)
hubTitle.BackgroundTransparency = 1; hubTitle.Font = Enum.Font.GothamBold; hubTitle.TextSize = 17
hubTitle.TextXAlignment = Enum.TextXAlignment.Left; hubTitle.TextColor3 = NK.WHITE
hubTitle.Text = "Hazee DUEL"; hubTitle.Parent = hubHeader
local hubTag = Instance.new("TextLabel")
hubTag.Size = UDim2.new(0, 200, 0, 16); hubTag.Position = UDim2.new(0, 52, 0, 34)
hubTag.BackgroundTransparency = 1; hubTag.Font = Enum.Font.Gotham; hubTag.TextSize = 10
hubTag.TextXAlignment = Enum.TextXAlignment.Left; hubTag.TextColor3 = NK.RED
hubTag.Text = "PHILIPPINES FLAG EDITION"; hubTag.Parent = hubHeader

-- Draggable hub
do
    local dragging, dragInput, dragStart, startPos = false, nil, nil, nil
    hubHeader.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; dragStart = input.Position
            startPos = hub.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    hubHeader.InputChanged:Connect(function(input)
        if (input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch) and dragging then
            local delta = input.Position - dragStart
            hub.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- SECTION TABS
local sections = { "Combat", "Player", "Visual", "Settings" }
local sectionButtons = {}
local frames = {}
local tabBarY = 58
local tabH = 30
local tabPad = 7
local tabW = (HUB_WIDTH - tabPad * (#sections + 1)) / #sections

local tabBar = Instance.new("Frame")
tabBar.Size = UDim2.new(1, 0, 0, tabH + 10)
tabBar.Position = UDim2.new(0, 0, 0, tabBarY)
tabBar.BackgroundTransparency = 1; tabBar.Parent = hub

for i, name in ipairs(sections) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, tabW, 0, tabH)
    btn.Position = UDim2.new(0, tabPad + (i-1)*(tabW+tabPad), 0.5, -tabH/2)
    btn.BackgroundColor3 = NK.PANEL; btn.BorderSizePixel = 0
    btn.Text = name; btn.Font = Enum.Font.GothamBold; btn.TextSize = 12
    btn.TextColor3 = NK.GREY; btn.Parent = tabBar
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 7)
    table.insert(sectionButtons, btn)

    local scroll = Instance.new("ScrollingFrame")
    scroll.Size = UDim2.new(1, -16, 1, -(tabBarY + tabH + 18))
    scroll.Position = UDim2.new(0, 8, 0, tabBarY + tabH + 10)
    scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    scroll.ScrollBarThickness = 3; scroll.ScrollBarImageColor3 = NK.BLUE
    scroll.BackgroundTransparency = 1; scroll.Visible = false
    scroll.Name = name .. "Frame"; scroll.Parent = hub
    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 7); layout.Parent = scroll
    layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 10)
    end)
    frames[name] = scroll

    btn.MouseButton1Click:Connect(function()
        for _, f in pairs(frames) do f.Visible = false end
        frames[name].Visible = true
        for _, b in pairs(sectionButtons) do b.BackgroundColor3 = NK.PANEL; b.TextColor3 = NK.GREY end
        btn.BackgroundColor3 = NK.BLUE; btn.TextColor3 = NK.WHITE
    end)
end

-- AUTO DUEL BUTTON (Combat section)
local duelBtnContainer = Instance.new("Frame")
duelBtnContainer.Size = UDim2.new(1, 0, 0, 52)
duelBtnContainer.BackgroundTransparency = 1; duelBtnContainer.Parent = frames["Combat"]
local duelActionBtn = Instance.new("TextButton")
duelActionBtn.Size = UDim2.new(1, 0, 1, 0)
duelActionBtn.BackgroundColor3 = NK.BLUE
duelActionBtn.Text = "⚔  Hazee DUEL  —  OFF"
duelActionBtn.Font = Enum.Font.GothamBold; duelActionBtn.TextSize = 14
duelActionBtn.TextColor3 = NK.WHITE; duelActionBtn.BorderSizePixel = 0
duelActionBtn.Parent = duelBtnContainer
Instance.new("UICorner", duelActionBtn).CornerRadius = UDim.new(0, 10)
local duelDot = Instance.new("Frame")
duelDot.Size = UDim2.new(0, 10, 0, 10); duelDot.Position = UDim2.new(0, 14, 0.5, -5)
duelDot.BackgroundColor3 = NK.GREY_DARK; duelDot.BorderSizePixel = 0; duelDot.Parent = duelActionBtn
Instance.new("UICorner", duelDot).CornerRadius = UDim.new(1, 0)
duelStatusLabel = Instance.new("TextLabel")
duelStatusLabel.Size = UDim2.new(1,-28,0,14); duelStatusLabel.Position = UDim2.new(0,28,1,-16)
duelStatusLabel.BackgroundTransparency = 1; duelStatusLabel.Text = "PHILIPPINES FLAG EDITION"
duelStatusLabel.Font = Enum.Font.Gotham; duelStatusLabel.TextSize = 10
duelStatusLabel.TextColor3 = NK.WHITE; duelStatusLabel.Parent = duelActionBtn
local function setDuelVisual(on, statusText)
    if on then
        TweenService:Create(duelActionBtn, TweenInfo.new(0.18), { BackgroundColor3 = Color3.fromRGB(0,40,130) }):Play()
        TweenService:Create(duelDot, TweenInfo.new(0.18), { BackgroundColor3 = NK.GREEN }):Play()
        duelActionBtn.Text = "⚔  Hazee DUEL  —  ON"
    else
        TweenService:Create(duelActionBtn, TweenInfo.new(0.18), { BackgroundColor3 = NK.BLUE }):Play()
        TweenService:Create(duelDot, TweenInfo.new(0.18), { BackgroundColor3 = NK.GREY_DARK }):Play()
        duelActionBtn.Text = "⚔  Hazee DUEL  —  OFF"
    end
    duelStatusLabel.Text = statusText or "PHILIPPINES FLAG EDITION"
end
duelActionBtn.MouseButton1Click:Connect(function()
    if DuelState.Moving then stopDuel(); setDuelVisual(false, "STOPPED.")
    else startDuel(); setDuelVisual(true, "RUNNING...") end
end)

-- ============================================================
-- TOGGLE FACTORY
-- ============================================================
local function CreateToggle(sectionName, text, onEnable, onDisable)
    local parentFrame = frames[sectionName]

    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, 0, 0, 46)
    container.BackgroundColor3 = NK.PANEL; container.BorderSizePixel = 0; container.Parent = parentFrame
    Instance.new("UICorner", container).CornerRadius = UDim.new(0, 10)
    local stroke = Instance.new("UIStroke", container); stroke.Color = NK.GREY_DARK

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.65, 0, 1, 0); label.Position = UDim2.new(0, 12, 0, 0)
    label.BackgroundTransparency = 1; label.Font = Enum.Font.GothamBold; label.TextSize = 13
    label.TextColor3 = NK.WHITE; label.TextXAlignment = Enum.TextXAlignment.Left
    label.Text = text; label.Parent = container

    local pill = Instance.new("TextButton")
    pill.Size = UDim2.new(0, 50, 0, 24); pill.Position = UDim2.new(1, -60, 0.5, -12)
    pill.BackgroundColor3 = NK.GREY_DARK; pill.Text = ""; pill.BorderSizePixel = 0; pill.Parent = container
    Instance.new("UICorner", pill).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 18, 0, 18); knob.Position = UDim2.new(0, 3, 0.5, -9)
    knob.BackgroundColor3 = NK.WHITE; knob.BorderSizePixel = 0; knob.Parent = pill
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local enabled = false
    pill.MouseButton1Click:Connect(function()
        enabled = not enabled
        if enabled then
            TweenService:Create(pill,      TweenInfo.new(0.2), { BackgroundColor3 = NK.BLUE }):Play()
            TweenService:Create(knob,      TweenInfo.new(0.2), { Position = UDim2.new(1, -21, 0.5, -9) }):Play()
            TweenService:Create(container, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(10, 18, 40) }):Play()
            stroke.Color = NK.BLUE
            if onEnable then onEnable() end
        else
            TweenService:Create(pill,      TweenInfo.new(0.2), { BackgroundColor3 = NK.GREY_DARK }):Play()
            TweenService:Create(knob,      TweenInfo.new(0.2), { Position = UDim2.new(0, 3, 0.5, -9) }):Play()
            TweenService:Create(container, TweenInfo.new(0.2), { BackgroundColor3 = NK.PANEL }):Play()
            stroke.Color = NK.GREY_DARK
            if onDisable then onDisable() end
        end
    end)
end

-- RANGE SETTING (textbox row)
local function CreateRangeSetting(sectionName, labelText, getValue, setValue, minV, maxV)
    local pf = frames[sectionName]
    local c = Instance.new("Frame"); c.Size = UDim2.new(1, 0, 0, 46)
    c.BackgroundColor3 = NK.PANEL; c.BorderSizePixel = 0; c.Parent = pf
    Instance.new("UICorner", c).CornerRadius = UDim.new(0, 10)
    Instance.new("UIStroke", c).Color = NK.GREY_DARK
    local l = Instance.new("TextLabel"); l.Size = UDim2.new(0.6, 0, 1, 0)
    l.Position = UDim2.new(0, 12, 0, 0); l.BackgroundTransparency = 1
    l.Font = Enum.Font.GothamBold; l.TextSize = 13; l.TextColor3 = NK.WHITE
    l.TextXAlignment = Enum.TextXAlignment.Left; l.Text = labelText; l.Parent = c
    local tb = Instance.new("TextBox"); tb.Size = UDim2.new(0, 78, 0, 28)
    tb.Position = UDim2.new(1, -88, 0.5, -14); tb.BackgroundColor3 = NK.CARD
    tb.TextColor3 = NK.WHITE; tb.Font = Enum.Font.GothamBold; tb.TextSize = 13
    tb.Text = tostring(getValue()); tb.ClearTextOnFocus = false; tb.BorderSizePixel = 0; tb.Parent = c
    Instance.new("UICorner", tb).CornerRadius = UDim.new(0, 6)
    Instance.new("UIStroke", tb).Color = NK.BLUE
    tb.FocusLost:Connect(function(e)
        if e then
            local n = tonumber(tb.Text)
            if n and n >= minV and n <= maxV then setValue(n)
            else tb.Text = tostring(getValue()) end
        end
    end)
end

-- ============================================================
-- POPULATE TOGGLES
-- ============================================================

-- COMBAT
CreateToggle("Combat", "Melee Aimbot",
    function() meleeEnabled = true; if character then createMeleeAimbot(character) end end,
    function() disableMeleeAimbot() end)

CreateToggle("Combat", "Auto Steal Nearest",
    function()
        autoStealEnabled = true
        createOrUpdateSquare(grabRadius)
        progressBarBg.Visible = true
        local sqConn = RunService.RenderStepped:Connect(updateSquarePosition)
        startSmartSteal()
        task.spawn(function()
            while autoStealEnabled do
                local char = lp.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                if root then
                    local targets = scanStealTargets()
                    local best, bestDist = nil, STEAL_GRAB_RADIUS
                    for _, t in ipairs(targets) do
                        local d = (root.Position - t.position).Magnitude
                        if d < bestDist then bestDist = d; best = t end
                    end
                    if best then
                        for i = 0, 1, 0.12 do
                            if not autoStealEnabled then break end
                            progressFill.Size = UDim2.new(i, 0, 1, 0)
                            percentLabel.Text = math.floor(i * 100) .. "%"
                            task.wait(0.03)
                        end
                        progressFill.Size = UDim2.new(0, 0, 1, 0); percentLabel.Text = "0%"
                    end
                end
                task.wait(0.3)
            end
            progressBarBg.Visible = false
            progressFill.Size = UDim2.new(0, 0, 1, 0); percentLabel.Text = "0%"
            hideSquare(); sqConn:Disconnect()
        end)
    end,
    function()
        stopSmartSteal()
        progressBarBg.Visible = false
        progressFill.Size = UDim2.new(0, 0, 1, 0); percentLabel.Text = "0%"
        hideSquare()
    end)

CreateToggle("Combat", "Lock Target",
    function() lockEnabled = true; startLock() end,
    function() lockEnabled = false; stopLock() end)

CreateToggle("Combat", "Auto Medusa",
    function() AutoMedusaEnabled = true; InitMedusa() end,
    function() AutoMedusaEnabled = false end)

CreateToggle("Combat", "Auto Bat",
    function()
        autoBatActive = true
        autoBatLoop = task.spawn(function()
            while autoBatActive do
                local char = lp.Character
                if char then
                    local tool = char:FindFirstChild("Bat")
                    if tool and tool:IsA("Tool") then pcall(function() tool:Activate() end) end
                end
                task.wait(0.4)
            end
        end)
    end,
    function() autoBatActive = false end)

CreateToggle("Combat", "Anti Sentry",
    function() startAntiSentry() end,
    function() stopAntiSentry() end)

-- PLAYER
CreateToggle("Player", "Speed Customizer",
    function()
        local bg = lp.PlayerGui:FindFirstChild("NorealBoosterCustomizer")
        if bg then bg.Enabled = true end
    end,
    function()
        local bg = lp.PlayerGui:FindFirstChild("NorealBoosterCustomizer")
        if bg then bg.Enabled = false end
    end)

CreateToggle("Player", "No Walk Animation",
    function() enableNoWalkAnim() end,
    function() disableNoWalkAnim() end)

CreateToggle("Player", "Anti Ragdoll",
    function() toggleAntiRagdoll(true) end,
    function() toggleAntiRagdoll(false) end)

CreateToggle("Player", "Spin Body",
    function() startSpinBody() end,
    function() stopSpinBody() end)

CreateToggle("Player", "Slow Fall",
    function() slowFallEnabled = true end,
    function() slowFallEnabled = false end)

CreateToggle("Player", "Infinite Jump",
    function() infiniteJumpEnabled = true end,
    function() infiniteJumpEnabled = false end)

-- VISUAL
CreateToggle("Visual", "ESP Players",
    function() toggleESPPlayers(true) end,
    function() toggleESPPlayers(false) end)

CreateToggle("Visual", "Anti Bee & Disco",
    function() enableAntiBee() end,
    function() disableAntiBee() end)

CreateToggle("Visual", "Xray Base",
    function() toggleESPBases(true) end,
    function() toggleESPBases(false) end)

CreateToggle("Visual", "Optimizer",
    function() enableOptimizer() end,
    function() disableOptimizer() end)

CreateToggle("Visual", "Anti FPS Devourer",
    function() enableAntiFPS() end,
    function() disableAntiFPS() end)

-- SETTINGS (range inputs)
CreateRangeSetting("Settings", "Lock Range",
    function() return LOCK_RADIUS end,
    function(n) LOCK_RADIUS = n end, 6, 500)

CreateRangeSetting("Settings", "Medusa Range",
    function() return MEDUSA_RADIUS end,
    function(n)
        MEDUSA_RADIUS = n
        if medusaPart then medusaPart.Size = Vector3.new(0.05, MEDUSA_RADIUS*2, MEDUSA_RADIUS*2) end
    end, 2, 200)

CreateRangeSetting("Settings", "Melee Range",
    function() return MELEE_RANGE end,
    function(n) MELEE_RANGE = n end, 2, 50)

CreateRangeSetting("Settings", "Steal Radius",
    function() return grabRadius end,
    function(n)
        grabRadius = n; STEAL_GRAB_RADIUS = n
        if stealSquarePart then stealSquarePart.Size = Vector3.new(0.05, grabRadius*2, grabRadius*2) end
    end, 5, 500)

-- ============================================================
-- SHOW FIRST TAB
-- ============================================================
frames["Combat"].Visible = true
sectionButtons[1].BackgroundColor3 = NK.BLUE
sectionButtons[1].TextColor3 = NK.WHITE

-- ============================================================
-- OPEN / CLOSE ANIMATION
-- ============================================================
local guiOpen = false
MobileToggle.MouseButton1Click:Connect(function()
    guiOpen = not guiOpen
    if guiOpen then
        TweenService:Create(hub, TweenInfo.new(0.28, Enum.EasingStyle.Quint),
            { Position = UDim2.new(0, 16, 0.5, -HUB_HEIGHT / 2) }):Play()
    else
        TweenService:Create(hub, TweenInfo.new(0.28, Enum.EasingStyle.Quint),
            { Position = UDim2.new(0, -(HUB_WIDTH + 20), 0.5, -HUB_HEIGHT / 2) }):Play()
    end
end)

-- Keybind: RightShift toggles hub
UserInputService.InputBegan:Connect(function(input, processed)
    if processed then return end
    if input.KeyCode == Enum.KeyCode.RightShift then
        guiOpen = not guiOpen
        if guiOpen then
            TweenService:Create(hub, TweenInfo.new(0.28, Enum.EasingStyle.Quint),
                { Position = UDim2.new(0, 16, 0.5, -HUB_HEIGHT / 2) }):Play()
        else
            TweenService:Create(hub, TweenInfo.new(0.28, Enum.EasingStyle.Quint),
                { Position = UDim2.new(0, -(HUB_WIDTH + 20), 0.5, -HUB_HEIGHT / 2) }):Play()
        end
    end
end)
